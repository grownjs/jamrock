const Jamrock = (typeof window !== 'undefined' && window.Jamrock) || {};

export const LiveSocket = Jamrock.LiveSocket;
export const Fragment = Jamrock.Fragment;
export const Browser = Jamrock.Browser;

export default Jamrock;
