import { Store } from '../dist/main.mjs';

export const writable = Store.writable;
export const readable = Store.readable;
export const connect = Store.connect;
export const session = Store.session;
export const derived = Store.derived;
export const get = Store.get;
