#!/usr/bin/env winterjs
import('../lib/winterjs/main.mjs').then(env =>
  import('./cli.mjs').then(cli => {
    const argv = typeof process !== 'undefined' ? process.argv.slice(2) : [];
    return cli.default(env.default, argv);
  })
).catch(console.error);
